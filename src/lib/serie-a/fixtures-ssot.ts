/**
 * Serie A confirmed-result SSOT overrides.
 * Empty stub — no fabricated fixtures, scores, or dates.
 */

import type { PlFixtureRow } from "@/lib/pl/types";

export function getSerieASsotFixtures(): PlFixtureRow[] {
  return [];
}
